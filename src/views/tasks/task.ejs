<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <%- include('../layouts/header', { isGuest: isGuest }) %>
                <% if (messages.success_msg) { %>
                 <div class="alert alert-success" role="alert">
                    <%= messages.success_msg %>
                </div>
                <% } %>
                <% if (messages.error_msg) { %>
                 <div class="alert alert-danger" role="alert">
                    <%= messages.error_msg %>
                 </div>
                <% } %>
                 <h1 class="mb-4"><%= task.name %></h1>
                <div class="container mt-4 shadow p-3 mb-5 bg-body rounded">
                    <div>
                        <div class="card-body d-flex justify-content-between p-3 ">
                            <div class="me-3">
                                <p class="card-text fs-5"><%= task.description %></p>
                            </div>
                            <div class="border-start p-3">
                                <div class="d-flex">
                                    <span class="badge bg-danger"><%= task.status_name %></span>
                                </div>
                                <p class="mt-3">
                                    <strong>Author:</strong> <%= task.creator_first_name %> <%= task.creator_last_name %> <br>
                                    <strong>Executor:</strong> <%= task.exc_first_name %> <%= task.exc_last_name %> <br>
                                    <strong>Created:</strong> <%= task.created_at %> 
                                </p>
                                <div class="d-flex gap-2">
                                    <button class="btn btn-primary"><a class="nav-link px-2 link-light" href="/tasks/<%= task.id %>/edit">Change</a></button>
                                    <form action="/tasks/<%= task.id %>?_method=DELETE" method="POST">
                                    <button class="btn btn-danger">Delete</button>
                                    </form>
                                </div>
            
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>